{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions needed by the three DeskMate windows.",
  "windows": ["settings", "pet", "bubble"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-hide",
    "core:window:allow-start-dragging"
  ]
}
